import { ClickableTableRowDirective } from './clickable-table-row.directive';

describe('ClickableTableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableTableRowDirective();
    expect(directive).toBeTruthy();
  });
});
